image: kkarczmarczyk/node-yarn:latest
before_script:
  - cd jalas-app
stages:
  - build
  - test
  - deploy

cache:
  paths:
  - jalas-app/node_modules/

build:
  stage: build
  script:
    - yarn install
    - yarn build

test:
  stage: test
  script:
    - echo "Running react test suite......"

